import 'dart:io';

class ScrollExtentContext {
  final double currentExtent;
  final double maxExtent;

  const ScrollExtentContext({
    required this.currentExtent,
    required this.maxExtent
  });
}